# Evaluation

> Placeholder. Populated by `python -m backend.learner_model.evaluate` in
> build-order step 7 (CLAUDE.md section 12). Will contain **real plots, not
> placeholders**, comparing SM-2 vs. HLR vs. a naive baseline on the learner
> simulator, plus at least one metric validated against logged real sessions.

Planned contents:

- Recall accuracy over simulated time (line plot per strategy)
- Calibration: predicted vs. actual recall probability — Brier score, log-loss, AUC
- Review efficiency: words retained per review issued
- Tool-call success / recovery rate from the failure-injection suite (section 11)
