# Failure recovery

> Generated by `python -m backend.agent.recovery` -- deterministic, scripted
> LLM (no API key). Fault injection: `backend/agent/failure_injection.py`.

## Summary

- Scenarios run: **7**
- Faults injected: **7**
- Tool-call success rate: **62%** (8/13 agent tool calls returned a usable result)
- Recovery rate (recovered or clarified): **71%**
- Graceful-handling rate (never crashed or hung): **100%**
- Dispositions: 4 recovered · 1 clarified · 2 failed gracefully · 0 crashed · 0 hung

## Per scenario

| Scenario | Injected fault | Disposition | Faults | Tool calls (ok/total) | Agent response |
|---|---|---|---|---|---|
| timeout, retried once | create_learning_session times out on the 1st call | **recovered** | 1 | 1/2 | Composed after one retry. |
| timeout, persistent | create_learning_session times out every call | **failed_gracefully** | 2 | 0/2 | The scheduler isn't responding right now, so I couldn't build a session. Please try again in a minute. |
| malformed response, retried | get_user_profile returns junk on the 1st call | **recovered** | 1 | 3/3 | Profile re-fetched, session composed. |
| malformed response, worked around | get_user_profile returns junk every call | **recovered** | 1 | 2/2 | Session composed without the profile. |
| ambiguous request, clarified | request stripped of cues: 'I have some time for German' | **clarified** | 0 | 0/0 | How many minutes do you have, and any particular focus (work, travel, exam)? |
| ambiguous request, safe default | request stripped of cues: 'I have some time for German' | **recovered** | 0 | 1/1 | Here's a 10-minute session. |
| cross-server: notes down | log_progress times out every call (server #2) | **failed_gracefully** | 2 | 1/3 | Your session is ready. I couldn't save a progress note -- the notes service isn't responding. |

