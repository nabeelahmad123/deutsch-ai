# Screenshots for the README

Drop PNGs here with these exact names (the README links them):

| file | what to capture |
|---|---|
| `coach.png` | The **Coach** view after a request completes — the reply, the intent chips, and the "How it did it" list of MCP tool calls visible. This is the headline image. |
| `session.png` | A study session mid-flow (a flashcard + the feedback). Optional. |
| `architecture.png` | Optional — a rendered version of the diagram in `docs/ARCHITECTURE.md` (export from mermaid.live if you want a raster copy). |

Keep them ~1200px wide, light theme, no personal data in view.
